<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="MainMenuSeperatorCT" TargetType="Separator">
        <Grid Height="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="27" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="23" />
            </Grid.ColumnDefinitions>
            <Border x:Name="IconBorder" Background="{DynamicResource MainMenuPopupBackgroundOfImage}" BorderThickness="1,0,0,0" BorderBrush="{DynamicResource MainMenuPopupBackgroundOfImage}" />
            <Border x:Name="Seper" Height="1" Background="{DynamicResource MainMenuBorderIsOpen}" Margin="6,1" Grid.Column="1" Grid.ColumnSpan="3"/>
        </Grid>

    </ControlTemplate>

</ResourceDictionary>